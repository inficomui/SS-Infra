import configureReactotron from './scripts/reactotron-config';

// Initialize Reactotron for this specific app
const reactotron = configureReactotron({
    name: 'SS-Infra Mobile',
});

export default reactotron;
